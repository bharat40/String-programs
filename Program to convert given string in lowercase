#include <stdio.h>
#include <ctype.h>
#include <string.h>
int main()
{
    char str[1000];
    printf("Enter your name in uppercase - ");
    fgets(str, 1000, stdin);
    int i = 0;

    while (str[i])
    {
        putchar(tolower(str[i]));
        i++;
    }

    return 0;
}
