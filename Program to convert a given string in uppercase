#include <stdio.h>
#include <ctype.h>
#include <string.h>
int main()
{
    char str[1000];
    printf("Enter your name in lowercase - ");
    fgets(str, 1000, stdin);
    int i = 0;

    while (str[i])
    {
        putchar(toupper(str[i]));
        i++;
    }

    return 0;
}
